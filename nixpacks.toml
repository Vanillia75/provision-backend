[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["python3", "tesseract", "poppler_utils"]

[deploy]
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
